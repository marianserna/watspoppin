<div className="form_outer">
  <div class="form_background">
    <div class="topColor"></div>
    <div class="bottomColor"></div>
  </div>

  <section class="login_form">
    <h2>Forgot your password?</h2>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: {class: "login", method: :post }) do |f| %>
      <%= devise_error_messages! %>

      <%= f.email_field :email, autofocus: true, placeholder: 'ENTER ACCOUNT EMAIL', class: "field" %>

      <div class="actions">
        <%= f.submit "Send me password reset" %>
      </div>
    <% end %>

    <%# render "devise/shared/links" %>
  </section>
</div>
